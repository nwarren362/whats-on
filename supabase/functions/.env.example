# Generate a long, random value and store it as CAPTURE_TOKEN in Supabase.
# Put the same value in the private Apple Shortcut configuration.
# Never commit the real token.
CAPTURE_TOKEN=replace-with-a-long-random-token
